<!DOCTYPE HTML>
<html>
<head>
<title>Home Page</title>
</head>
<body class="home">
<nav class="navbar navbar-default navbar-fixed-top navbar-inverse">
<div class="container-fluid">
  <% if user_signed_in? %>
<div class="dropdown">
  <button class="btn btn-default dropdown-toggle" type="button" id="dropdownMenu1" data-toggle="dropdown">
    <%= current_user.username %>
    <span class="caret"></span>
  </button>
  <% if current_user.admin == true %>
  <ul class="dropdown-menu" role="menu" aria-labelledby="dropdownMenu1">
    <li role="presentation"><%= link_to "Admin Dashboard", admin_dashboard_path,tabindex:"-1", role:"menuitem" %></li>
    <% end %> 
    <li role="presentation"><%= link_to "Sign Out", destroy_user_session_path, method: :delete, tabindex:"-1", role:"menuitem" %></li>
  </ul>
</div>

    
  <% else %>
    <p class="navbar-text navbar-left"><%= link_to "Sign in", new_user_session_path, class:"navbar-link" %></p>
<% end %>
</div>
</nav>
<%= render 'homes/shared/head' %>
<div class="container">
<% n = 1 %>
<% @posts.each do |p| %>

Post <%= n %> </br> 
Title: <%= p.title %> </br>
Content: <%= p.content %></br>
<%= link_to "show details", controller: "homes", action: "show", id: p.id %>
</br>
<% n += 1 %>
<% end %>
</div>

</body>
</html>