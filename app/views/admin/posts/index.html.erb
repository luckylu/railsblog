    <% flash.each do |name, msg| -%>
      <%= content_tag :div, msg, class: name %>
    <% end %>
<%= link_to "New", new_admin_post_path %> 
</br>
<% n = 1 %>
<table class="table table-striped table-bordered table-condensed table-hover">
  <tbody class="table">
    <tr>
      <th> Post # </th>
      <th> Title </th>
      <th> Content </th>
      <th> Operation </th>
    </tr>
    
    <% @posts.each do |p| %>
    <tr>
      <td><%= n %> </td> 
      <td><%= p.title %> </td>
      <td><%= p.content %></td>
      <td><%= link_to "Edit", edit_admin_post_path(p) %> | <%= link_to "Show", controller: "/homes", action: "show", id: p.id %> | <%= link_to "Delete", admin_post_path(p), method: :delete %></td>
    </tr>
<% n += 1 %>
<% end %>
  </tbody>
</table>
<div class="flickr_pagination">
    <div class="page_info">
      <%= page_entries_info @posts %>
    </div>
    <%= will_paginate @posts, :container => false %>
  </div>

