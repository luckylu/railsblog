<!DOCTYPE HTML>
<html>
<head>
<title>Comments</title>
</head>
<body>
<%= render "admin/shared/head" %>
<%= render "admin/shared/sidebar" %>
<div class="container">
<% @comments.each do |c| %>
<%= c.content %> | <%= link_to "Delete", admin_comment_path(c.id), method: :delete %></br>
<% end %>
</div>
</body>
</html>